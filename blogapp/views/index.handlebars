<div class="bg-light p-5 rounded-3 shadow-sm mb-4">
    <div class="container">
        <h1 class="display-4">Bem-vindo ao Blog do Node</h1>
        <p class="lead">Um exemplo de texto que descreve mais sobre o conteúdo dessa seção.</p>
        <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
    </div>
</div>

<hr>

<h2>Postagens Recentes:</h2>

{{#each postagens}}
    <div class="card">
        <div class="card-body">
            
            <h3>{{this.titulo}}</h3>
            <p>{{this.descricao}}</p>
            <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais...</button></a>
            <hr></hr>
            <small>Categoria: {{this.nome}}</small><br>
            <small>Data de publicação: {{this.data}}</small>
        </div>
    </div>
{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}

