<div class="card">
    <div class="card-body">
        <h1>{{titulo}}</h1>
        <hr>
        <small>Data de publicação: {{dateFormat data "DD/MM/YYYY"}}</small><br>

        <hr>
        {{conteudo}}
    </div>
</div>

